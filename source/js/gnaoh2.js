(function (win, doc) {
    var developement = true;
    var log = developement ? function (args) {
            win.console.log(args);
        } : function () {};
    //backbone
    
})(window, document);